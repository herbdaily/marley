
=== Secrets

setup
  @client=Marley::TestClient.new(:resource_name => 'secret')
end


examples:

>> @client.auth=@user1_auth
=> ["user1", "asdfasdf"]

>> @secret=@client.read({}, :method => 'new')
=> [:instance, {:new_rec=>true, :actions=>nil, :name=>"secret", :schema=>[["integer", :user_id, 1, 1], ["text", :name, 4, nil], ["text", :message, 0, nil], [:text, :private_tags, 0, nil]], :url=>"/secret/"}, []]

!!!


